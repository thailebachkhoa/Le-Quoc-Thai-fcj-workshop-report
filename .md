

Plantify worklog · MD
# Worklog — Hành trình triển khai Plantify Co lên AWS
 
Tài liệu này ghi lại đúng trình tự **thực tế** đã làm, bao gồm cả những chỗ phải quay lại sửa, chọn sai rồi đổi hướng, hoặc phát hiện lỗi giữa chừng. Không phải phiên bản "lý tưởng hoá".
 
---
 
## Dịch vụ / công cụ đã dùng
 
| Dịch vụ | Vai trò |
|---|---|
| **Amazon EC2** | Server chạy Apache + PHP 8.5 + ffmpeg, host toàn bộ web |
| **Amazon RDS (MySQL)** | Database quan hệ, `Public access: No` |
| **Amazon S3** | Lưu bản sao lưu (backup) database hàng ngày |
| **AWS IAM** | Role + Policy Least Privilege cho EC2 (→ S3) và Lambda (→ EC2/RDS) |
| **Amazon CloudWatch** | Alarm giám sát CPU (EC2) và dung lượng ổ đĩa (RDS) |
| **Amazon SNS** | Gửi email cảnh báo khi Alarm kích hoạt |
| **AWS Lambda** | Chạy logic Start/Stop tài nguyên tự động |
| **Amazon EventBridge Scheduler** | 4 lịch trình (bật/tắt EC2+RDS theo khung giờ sử dụng) |
| **Amazon Cognito** | User Pool + Hosted UI, cầu nối OAuth sang Google, quản lý Group Admin/Member |
| **Google Cloud (OAuth Client)** | Identity Provider thật — xác thực tài khoản Google |
| **AWS CloudShell** | Chạy AWS CLI để debug khi cần (dùng thay AWS CLI lỗi trên máy local) |
| **GitHub** | Lưu code, là nguồn deploy duy nhất (`git pull` tay trên EC2) |
| **DuckDNS** | Domain miễn phí trỏ về EC2 (thay Route 53 để tiết kiệm chi phí) |
| **Let's Encrypt / Certbot** | Chứng chỉ HTTPS miễn phí |
| **Google Authenticator (TOTP tự viết)** | Lớp xác thực 2 bước bắt buộc riêng cho Admin |
 
---
 
## Giai đoạn 0 — Rà soát và vá bảo mật code (trước khi đụng tới AWS)
 
Trước khi triển khai, review toàn bộ codebase PHP thuần (MVC tự viết) và phát hiện một loạt vấn đề cần xử lý trước:
 
- **Không có CSRF protection** ở bất kỳ action ghi/xóa nào (Admin, Auth, Cart, Shop, News, Dashboard) — xây dựng class `Csrf` dùng chung, áp dụng qua constructor của từng Controller, đổi toàn bộ link `<a href>` phá hủy (xóa user, xóa bài viết...) sang `<form method="POST">`.
- **Reset password mặc định yếu** (`123456` cố định) — ghi nhận, cần cải thiện sau.
- **Lộ chi tiết lỗi hệ thống** ra giao diện (`Database.php`, `Order.php` in thẳng exception message) — sửa thành ghi `error_log()`, hiển thị thông báo chung.
- **Bug hiển thị `?>?>` lặp đôi** trong `header.php`/`dashboard/index.php` — in ra ký tự thừa ở đầu mọi trang.
- **`composer.json` sai case** (`bootstrap.php` thay vì `Bootstrap.php`) — gây lỗi trên Linux (case-sensitive) dù chạy được trên Windows.
- **`schema.sql`** dư dấu phẩy trước `ON DUPLICATE KEY UPDATE` — sẽ lỗi cú pháp khi import trên MySQL thật.
- File upload video hero hoá ra chỉ phục vụ riêng cho trang About (phát hiện muộn hơn, ở Giai đoạn 6).
---
 
## Giai đoạn 1 — Dựng hạ tầng AWS cơ bản (EC2 + RDS)
 
1. Tạo EC2 (`t3.micro`, Ubuntu) — vướng ngay từ đầu: tài khoản AWS **chưa có VPC** trong region `ap-southeast-1`, phải tạo Default VPC trước khi Launch instance được.
2. Cấu hình Security Group: SSH giới hạn theo "My IP", HTTP/HTTPS mở public.
3. SSH vào EC2 — vướng nhiều lần: gõ `yes` xác nhận fingerprint bị lỗi lặp trên WSL, phải dùng `-o StrictHostKeyChecking=no`; sau đó phát hiện `chmod 400` không có tác dụng vì file key đang nằm ở ổ NTFS Windows (`/mnt/d/...`) — phải copy key vào `~/.ssh/` (filesystem Linux thật trong WSL) mới chạy đúng.
4. Cài Apache, PHP 8.5, ffmpeg, Git trên EC2.
5. Clone code từ GitHub vào `/var/www/plantify`, cấu hình Apache VirtualHost trỏ đúng vào `public/` (không phải root project, tránh lộ `.env`), bật `mod_rewrite`.
6. Tạo RDS MySQL — thiếu MySQL client trên EC2 (`mysql: command not found`), phải cài `mysql-client-core` trước khi kết nối test được.
7. Quên tạo Database ban đầu (`Initial database name` bỏ trống lúc tạo RDS) — phải tự `CREATE DATABASE plantify` tay.
8. Import `schema.sql` — sai đường dẫn lần đầu (`database/schema.sql` không tồn tại, file thật nằm ở `database/migrations/schema.sql`).
9. Kết nối EC2 ↔ RDS: ban đầu Security Group của RDS không cho phép EC2 truy cập — dùng tính năng **"Set up EC2 connection"** có sẵn trong RDS Console để AWS tự thêm đúng rule, thay vì tự sửa Security Group tay.
10. Test web qua Public IP — thành công, xác nhận toàn bộ chuỗi Apache → PHP → RDS hoạt động.
---
 
## Giai đoạn 2 — Mở rộng dịch vụ AWS (S3, IAM, CloudWatch)
 
1. Tạo IAM Role (`PlantifyEC2Role`) + Policy tùy chỉnh (Least Privilege: chỉ `PutObject`/`GetObject`/`ListBucket` trên đúng 1 bucket) — gắn vào EC2 qua **Modify IAM role**.
2. Tạo S3 bucket backup — **tạo nhầm region US East (N. Virginia)** lần đầu, phải xóa và tạo lại đúng `ap-southeast-1` (Singapore) để khớp region với EC2/RDS.
3. Viết script `backup-db.sh` (`mysqldump` + `aws s3 cp`), cấp quyền thực thi — gặp lỗi Permission denied do file bị tạo qua `nano` dính ký tự ẩn, phải tạo lại bằng `cat heredoc`.
4. Thiết lập `cron` chạy backup hàng ngày.
5. Tạo CloudWatch Alarm cho EC2 (CPU > 80%) và RDS (FreeStorageSpace) + SNS gửi email — **set sai chiều điều kiện lần đầu** (Greater Than thay vì Lower Than), khiến Alarm báo động giả dù ổ đĩa còn dư ~19.5GB; phát hiện qua nội dung email cảnh báo, sửa lại đúng chiều.
---
 
## Giai đoạn 3 — Domain, HTTPS, Elastic IP
 
1. Gắn **Elastic IP** để cố định địa chỉ — trước đó Public IP của EC2 đổi liên tục mỗi lần Stop/Start, gây SSH bị treo/timeout nhiều lần do Security Group vẫn whitelist IP cũ.
2. Dùng **nip.io** làm domain tạm thời (miễn phí, không cần đăng ký) để đủ điều kiện cấp SSL, sau đó chuyển hẳn sang **DuckDNS** làm domain chính thức.
3. Cài Certbot, bật HTTPS qua Let's Encrypt — redirect toàn bộ HTTP sang HTTPS.
4. Đính chính lại hiểu nhầm ban đầu: Elastic IP **không còn miễn phí tuyệt đối** từ chính sách AWS 2024 (mọi IPv4 công khai tính phí $0.005/giờ), chỉ miễn phí nếu tài khoản còn trong Free Tier 12 tháng đầu và chỉ có đúng 1 địa chỉ IP.
---
 
## Giai đoạn 4 — Tự động hoá tiết kiệm chi phí (Lambda + EventBridge)
 
1. Viết Lambda function (`plantify-scheduler`) nhận tham số `action` (`start`/`stop`), gọi `boto3` điều khiển cả EC2 lẫn RDS, có xử lý `try/except` để không lỗi khi gọi trùng trạng thái.
2. Tạo IAM Role riêng cho Lambda (`PlantifySchedulerRole`), Policy giới hạn đúng instance/DB cụ thể.
3. Test lần đầu bị **lỗi timeout** (mặc định Lambda chỉ 3 giây, gọi 2 API AWS mất hơn) — tăng lên 30 giây.
4. Tạo 4 lịch **EventBridge Scheduler** (bật 08:00, tắt 17:30, bật 20:00, tắt 00:00) — **nhầm timezone lần đầu** (tưởng cần quy đổi sang UTC, nhưng lịch đã tự để sẵn UTC+07:00 Sài Gòn) khiến lịch test không chạy đúng giờ dự kiến; sửa lại dùng thẳng giờ Việt Nam.
5. Xác nhận hoạt động đúng bằng cách test thủ công với giờ gần hiện tại trước khi tin tưởng chạy tự động.
---
 
## Giai đoạn 5 — Chuyển đổi Auth sang Cognito + Google OAuth + TOTP
 
### Thiết kế kiến trúc
Chốt luồng: user & admin cùng bấm "Đăng nhập Google" → Cognito xác thực qua Google → PHP đọc Cognito Group → Admin bị chặn thêm bước TOTP trước khi vào `/admin`.
 
*(Quyết định này thay thế hướng ban đầu — Cognito Email OTP thuần — sau khi cân nhắc và xác nhận lại yêu cầu thực tế là đăng nhập bằng tài khoản Google thật, không phải mã gửi qua email.)*
 
### Dựng hạ tầng AWS/Google (làm tay trên console)
1. Tạo Cognito User Pool + App Client.
2. Tạo Cognito Domain (Hosted UI).
3. Tạo Google OAuth Client (Google Cloud Console) — vướng khá nhiều: bị chặn quyền do dùng tài khoản tổ chức → phải đổi sang project tài khoản cá nhân; app ở chế độ Testing giới hạn 100 user → phải Publish.
4. Thêm Google làm Identity Provider trong Cognito, map thuộc tính `email`/`name`.
5. Bật Google trong App Client — **bước bị sót lâu nhất**: mãi mới phát hiện `SupportedIdentityProviders` chỉ có `COGNITO`, thiếu `Google`, gây lỗi `invalid_scope`/`invalid_request` khi thử đăng nhập.
### Viết code
Thêm `Cognito.php` (xử lý OAuth + verify JWT), `Totp.php` (TOTP tự viết, không phụ thuộc thư viện ngoài), sửa `AuthController`, `User` model, thêm view `auth/totp.php`, migration thêm cột `cognito_sub`/`totp_secret`, cho phép `password` NULL.
 
### Deploy & vá lỗi liên tục (phần tốn thời gian nhất)
- `composer install` chặn bởi lỗi bảo mật của `firebase/php-jwt` bản cũ → nâng lên v7.
- Quên mật khẩu RDS → reset qua Console.
- File bị đặt sai tên (`totp.php` → `toth.php`), file `login.php` quên push lên GitHub.
- Lỗi `invalid_scope` → thiếu tick `profile` trong OAuth scopes của App Client.
- Lỗi 400 mơ hồ từ Cognito → dò qua CloudShell mới phát hiện App Client thiếu `Google` trong danh sách Identity Provider được phép.
- Lỗi 500 cuối cùng → bug thật trong code (`Database` là singleton, gọi lồng 2 câu SQL cùng lúc làm lệch tham số bind).
### Kết quả
Đăng nhập Google qua Cognito và TOTP cho Admin đã chạy ổn định.
 
---
 
## Giai đoạn 6 — Sửa lỗi UI mobile & dọn dẹp code thừa (gần nhất)
 
### Sửa lỗi hiển thị trên mobile
Phát hiện chữ ở hero section bị cắt mất ký tự đầu dòng trên màn hình điện thoại (390px). Nguyên nhân: Bootstrap `.row.g-5` (gutter 24px) vượt quá padding mặc định của `.container` (12px), phần dư tràn ra ngoài và bị section cha (`overflow: hidden`) cắt cụt. Sửa 7 vị trí trong 3 file (`home.php`, `faq.php`, `product-detail.php`) sang gutter responsive (`gy-5 gx-3 gx-lg-5`), đồng thời thêm `overflow-x: hidden` ở `html, body` làm lưới an toàn chung.
 
### Dọn dẹp "About page" cũ — ĐANG DỞ DANG
Do đã tách nội dung trang About cũ vào các section trong trang chủ, phần quản trị (`admin/pages.php`, `public/api/upload-video.php`) trở thành "mồ côi" — có route ẩn không hiện trên UI nhưng vẫn truy cập được nếu gõ tay URL. Đã xóa 2 file view đó, nhưng **chưa hoàn tất**:
- `AdminController::pages()` và `save_pages()` vẫn còn tồn tại, sẽ báo lỗi "View not found" nếu ai gõ đúng URL `/admin/pages`.
- `Sidebar.php` vẫn còn `'pages'` trong danh sách route (không ảnh hưởng chức năng, chỉ là dọn dẹp).
- `home.php` vẫn đọc key `about.process_*`/`about.map_*` cũ thay vì `home.process_*`/`home.map_*` — khiến 2 khối "Quy trình" và "Vị trí" trên trang chủ **hiện đúng nhờ giá trị mặc định hard-code**, nhưng **không còn cách nào chỉnh sửa text qua admin** vì editor cũ đã xóa, editor mới (`page_home`) chưa được bổ sung 2 khối này.
*(Việc cần làm tiếp: hoàn tất 3 điểm trên — patch cụ thể đã có sẵn, chỉ chờ áp dụng.)*
 
---
 
## Việc còn lại cho báo cáo cuối cùng
 
- [ ] Hoàn tất dọn dẹp About page (Giai đoạn 6)
- [ ] Bản tài liệu song ngữ Việt-Anh
- [ ] Reflection cá nhân (khó khăn gặp phải, hướng phát triển)
- [ ] 3 bài blog post cho AWS Study Group
 












































